/**
 * SEO metadata helpers — native/default no-op (web implementation in seoMetadata.web.js).
 */

export function applySeoPageMeta() {
  // No document head on native.
}

export function resetSeoPageMeta() {
  // No document head on native.
}
