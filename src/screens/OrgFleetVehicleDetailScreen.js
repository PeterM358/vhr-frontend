/**
 * Org fleet vehicle detail — same UI as client VehicleDetailScreen.
 * Route params: organizationId, vehicleId (and optional backLabel).
 * VehicleDetailScreen reads organizationId and switches chrome / returnTo.
 */
export { default } from './VehicleDetailScreen';
