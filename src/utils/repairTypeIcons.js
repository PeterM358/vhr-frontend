/**
 * @deprecated Import from `src/icons/operationIconRegistry` instead.
 * Thin re-export so existing call sites keep working during migration.
 */
export {
  getOperationIcon as resolveRepairTypeIcon,
  getOperationIcon,
  resolveOperationIconKey,
  DEFAULT_REPAIR_TYPE_ICON,
  GENERIC_SERVICE_ICON_KEY,
} from '../icons/operationIconRegistry';
