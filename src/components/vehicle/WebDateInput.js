import React from 'react';
import { View } from 'react-native';

/** Native/Android/iOS: date input is handled in VehicleCollapsibleFormSections. */
export default function WebDateInput() {
  return <View />;
}
